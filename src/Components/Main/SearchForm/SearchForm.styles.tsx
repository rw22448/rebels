import styled from 'styled-components';

const StyledForm = styled.form``;

const SearchHeader = styled.h1`
  font-size: 80px;
`;

const SearchDescription = styled.p`
  font-size: 16px;
`;

export { StyledForm, SearchHeader, SearchDescription };
